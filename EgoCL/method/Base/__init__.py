from .Method import Method
from .Respond import Respond, Retrieve
from .Memorize import Memorize, Memory
#currently, don't write anything in these base classes,
#Because I'm not quite clear about the structure yet, so write too much things now might cause confliction in the future, or make things hard to change later.
#Maybe in the future, when I'm quite sure about which functions should be included in the base class, I will add them later.